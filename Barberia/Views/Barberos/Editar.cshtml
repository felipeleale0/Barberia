@* @model Barberia.Models.Barbero

@{
    ViewData["Title"] = "Editar Barbero";
}

<div class="contenedor">
    <h1 class="titulo"><i class="fa-solid fa-user-pen"></i> Editar Barbero</h1>

    <form asp-action="Editar" method="post" class="formulario">
        <input type="hidden" asp-for="Id" />

        <div class="grupo">
            <label>Nombre</label>
            <input asp-for="Nombre" class="input" required />
        </div>

        <div class="grupo">
            <label>Especialidad</label>
            <input asp-for="Especialidad" class="input" required />
        </div>

        <div class="grupo">
            <label>Años de Experiencia</label>
            <input asp-for="AñosExperiencia" type="number" class="input" min="0" required />
        </div>

        <div class="botones">
            <button type="submit" class="boton">Guardar Cambios</button>
            <a asp-action="Index" class="boton cancelar">Cancelar</a>
        </div>
    </form>
</div>

<style>
    body {
        background-color: #111;
        color: #f8f8f8;
        font-family: 'Poppins', sans-serif;
        text-align: center;
    }

    .contenedor {
        margin: 40px auto;
        width: 90%;
        max-width: 500px;
        background-color: #1b1b1b;
        border-radius: 20px;
        padding: 30px;
        box-shadow: 0 0 20px rgba(255, 215, 0, 0.2);
    }

    .titulo {
        color: #ffd700;
        margin-bottom: 25px;
    }

    .formulario .grupo {
        margin-bottom: 15px;
        text-align: left;
    }

    .formulario label {
        color: #ffd700;
        font-weight: bold;
        display: block;
        margin-bottom: 5px;
    }

    .input {
        width: 100%;
        padding: 10px;
        border-radius: 8px;
        border: none;
        background-color: #222;
        color: #fff;
        font-size: 1em;
    }

    .botones {
        margin-top: 20px;
        display: flex;
        justify-content: space-between;
    }

    .boton {
        background-color: #ffd700;
        color: #000;
        padding: 10px 20px;
        border-radius: 10px;
        border: none;
        text-decoration: none;
        font-weight: bold;
        cursor: pointer;
        transition: 0.3s;
    }

        .boton:hover {
            background-color: #fff200;
        }

    .cancelar {
        background-color: #444;
        color: white;
    }

        .cancelar:hover {
            background-color: #666;
        }
</style> *@
